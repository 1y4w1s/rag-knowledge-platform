# 2026-07-13 知岸 RAG · 全站代码同步（预览↔代码收口）

## 2026-07-13 下午：Dashboard 预览→代码同步（Task #78 ✅）
- 已完成真实 vs 预览左右对比：`.workbuddy/dash_compare.py` 登录 demo_admin 后截取真实 `/dashboard` 与 `docs/dashboard-warm-white-preview.html`。
- 最大可落差异：**统计卡片视觉**（真实灰图标、预览彩色图标背景 + 彩色数值）。
- 已改造 `frontend/src/components/dashboard/StatCard.tsx`：
  - 新增 `iconTone: clay|blue|green|rose|slate`，每种 tone 带浅色背景 + 同色系文字 + 对应 ring。
  - 图标容器从 18px 裸图标改为 36px 圆角底块。
  - 数值按 `value.toLocaleString('zh-CN')` 格式化，并随 tone 着色。
  - 骨架同步为 36px 占位块。
- `DashboardStatsGrid` 为 4 张卡片分别指定 tone：资料库 clay、已上传文件 blue、已可提问文件 green、近7日提问 rose。
- 验证：`npm run build` 通过，`npm run test` 28 passed；对比图 `.workbuddy/dash_compare_v3.png` 已生成。
- 剩余差异：
  - 侧栏导航项数量因当前在 **personal 空间**而少于预览的团队空间视图；切换 team 空间后会自动显示「组织与部门 / 成员管理 / 团队设置 / 操作审计」。
  - 搜索区/状态区/运营指标因数据状态不同而内容不同，但组件结构已对齐。

---

## 2026-07-13 下午：Dashboard 预览→代码同步 + 12 维自评（Task #78 ✅）
- 已完成真实 vs 预览左右对比：临时 Playwright 脚本登录 demo_admin 后截取真实 `/dashboard` 与 `docs/dashboard-warm-white-preview.html`；截图已归档到 `docs/screenshots/dashboard_eval_*.png`。
- **已落回代码的改进**：
  - `StatCard`：`iconTone` 彩色图标底块 + 同色系数值 + 千分位 + hover 微上浮。
  - `DashboardStatsGrid`：4 张卡片分别 clay/blue/green/rose。
  - `SearchModeTabs`：从分段控件改为 pill 按钮，移动端强制横向不换行。
  - `DashboardZoneA`：上传/创建资料库按钮加图标。
  - `DashboardDocumentSearch`：搜索结果行加文件图标 + `aria-live` 播报。
  - `DashboardPage`：补 `document.title = "知岸 · 概览"`。
- 评估报告：`docs/dashboard_evaluation_report.md` → **9.05/10（A 级）**。
- 验证：`npm run build` 通过，`npm run test` 28 passed。
- 决策：Dashboard 单页已到 A 级，继续单页冲 9.5 边际收益低；按用户“没到 9.5 就继续”的指令，**转向提升整体项目分**：继续 Task #79 KBs 等剩余页面同步。

---

## 背景
- 评分卡（`redesign-scorecard.md` §4 / `strict-review-v2.md` §8 v3）显示 12 页渲染复核后加权均值 9.22、全 ≥9.0，但**反复标注"同步回代码"才是真正验收项**：预览已打磨到 9.2+，运行代码仍滞后（S1/S2 外壳透明度/内距明确"待代码同步"）。性能分为纯静态预览理论值，需真机 Lighthouse。
- 用户选定方向：**全站代码同步**（除 chat/ask 外约 10 页落回 React + 真机 Lighthouse）。

## 本轮完成：Task #77 全局无障碍地板同步（一次性提升全 12 页）
- `AppShellLayout.tsx`：新增 shell 级 `skip-link` 跳到 `#main`；`<main>` 加 `id="main" tabIndex={-1}` landmark。
- `sidebar-nav.tsx`：`<Link>` 激活项加 `aria-current="page"`（键盘/读屏可达当前页）。
- `index.css`：
  - 全局 `:focus-visible` 陶土焦点环，仅兜底自定义控件（`[role=option/tab/button/switch/tabpanel]`、`[tabindex]`），避免与原生控件已有 ring 双指示。
  - 全局 `prefers-reduced-motion` 守卫（`*` 动效→.001ms）。
- 去重：`AskPage`/`ChatPage` 的每页 `skip-link`（跳 #chat-input）移除，避免与 shell 全局重复（restraint 原则，对齐预览地板单一 skip-link）。
- 验证：`npm run build` 通过；`npm run test` 28 passed。

## 待办（Task #78–#84，按页同步）
- #78 Dashboard · #79 KBs · #80 KBDetail · #81 Members/Departments · #82 Account/OrgSettings · #83 AdminAudit/DocPreview：逐页对照 warm-white 预览 + scorecard，落回空态统一/微交互/自定义控件键盘导航/品牌空态插画；Dashboard 统计卡片漂浮已在代码修复（strict-review §7）。
- #84 真机 Lighthouse：全站同步后跑，坐实性能分。

## 关键事实
- 全局 CSS 变量 `--action` = 陶土色（#cb6b3d），skip-link/focus 环共用。
- 预览 v2.5 系统性 A11y 地板（focus-visible/reduced-motion/skip-link/landmark/aria-current）是本次代码同步的事实来源（`strict-review-v2.md` §2.1）。

## 查看方式 / 环境状态（用户问"怎么看、哪个页面"）
- 无障碍地板在 **AppShellLayout（外壳）** → **登录后任意页面**都可见（Dashboard 是登录后的落地页，最自然）。登录页本身不在外壳内，无 skip-link。
- **当前环境**：前端 dev server 在 `http://localhost:5173/`（HTTP 200 可达），后端 API 也已起（见下方"本环境拉起真实全栈"，8000 健康、demo_admin 可登录）。打开 5173 → 登录 → 进外壳看全 12 页 + 无障碍地板。
- 起全栈的正规方式（README）：`docker compose up -d --build`（Postgres+API），再前端 `npm run dev -- --port 5173`。登录：`demo-admin@example.com` / `password123`（来自 `backend/scripts/seed_enterprise_demo.py`）。
- 已建**自包含演示页** `docs/a11y-floor-demo.html`：原样搬入 `index.css` 的 skip-link/focus-visible/reduced-motion 规则 + 外壳结构，无需后端即可 Tab 实看效果。

## 本环境拉起真实全栈（用户要"真实的"，已验证可行）
- **Docker 守护进程默认没起**：先 `PowerShell Start-Process "C:\Program Files\Docker\Docker\Docker Desktop.exe"`，再轮询 `docker info` 直到 OK（约 8 次/16s）。
- 项目根 `docker compose up -d --build` → 构建 `zhiku-postgres`(5432) + `zhiku-api`(8000)。`api` 镜像 CMD 仅 `uvicorn`，**不会自动迁移/播种**。
- 必须手动：① `docker compose exec api alembic upgrade head`；② `docker cp backend/scripts/seed_enterprise_demo.py zhiku-api:/tmp/seed_enterprise_demo.py` 后 `docker compose exec api env PYTHONPATH=/app python /tmp/seed_enterprise_demo.py`（脚本放 /tmp 时须加 `PYTHONPATH=/app` 才能 import `app`）。
- `demo_admin` 落在 postgres 卷里会持久（重跑显示 already exists）。登录 `demo-admin@example.com` / `password123`。
- 验证：`curl localhost:8000/health` → `{"status":"ok","database":"ok"}`；`POST /api/v1/auth/login` 回 JWT。前端 dev server 仍跑 5173（proxy /api→8000）。
- 用户访问：`http://localhost:5173/login` → 登录 → 进外壳，可看全 12 页 + 无障碍地板。

---

## 2026-07-13 傍晚：清理冗余预览 HTML（用户指出"做了太多预览 html"）
- **问题**：`docs/` 下累计 43 个预览 HTML、31,701 行，绝大多数是重设计各阶段（ws2 / ux-p1 / dashboard v3→v4.3.2 / 品牌重设计 / 演示稿）的冗余迭代草稿，无人收尾。
- **已清理**：删除 28 个冗余草稿（`docs/dashboard-content-v3/v4/v4.1/v4.2/v4.3/v4.3.1/v4.3.2-preview.html`、`dashboard-ui-preview.html`、5 个 `preview-ux-p1-*`、6 个 `preview-workspace*/preview-dashboard-ws2-3/preview-kb-list-ws2-2/preview-sidebar-ws2-1/preview-register*/preview-login-p1-compare/preview-org-departments`、`cockpit/design-preview/brand-redesign-preview/preview-agent-platform/page-layout-evaluation/presentation-defense-w6-3`）。
- **保留 15 个 canonical**：`previews-gallery.html`（总索引）+ 12 页 `*warm-white-preview.html`（login/dashboard/knowledge-bases/knowledge-base-detail/chat/ask/members/org-departments/organization-settings/account-settings/admin-audit/document-preview）+ `auth-warm-orange-preview.html`（登录变体）+ `a11y-floor-demo.html`（无障碍功能演示）。
- **修断链**：5 个 md（DESIGN/G1/G2/G3/ORG_DEPARTMENTS_ACCEPTANCE）共 6 处硬链接指向已删文件，已改到 canonical：`cockpit.html`/`preview-agent-platform.html`→`previews-gallery.html`；`dashboard-content-v4.3.2-preview.html`→`dashboard-warm-white-preview.html`。复查无断链。
- **约定（防再堆）**：① 每页只保留 1 个 warm-white canonical 预览作"代码同步唯一参考源"；② 对比用 Playwright 截真实页面（`.workbuddy` 临时图，用后归档 `docs/screenshots/` 并清 `.workbuddy`）；③ **不再每轮迭代新建 HTML 预览稿**。Ardot 设计技能（ardot-design-core/router/ui-design）是画布工具，本项目实际产物是 React 代码，预览 HTML 仅作参考、不 proliferate。

---

## 2026-07-13 晚上：KBs 列表页同步收尾（Task #79 ✅）
- 从上一回合 v5 中断处继续：已完成 `KnowledgeBaseCard`/`KbListSearchBar`/`KnowledgeBasesPage` 的暖白预览同步，但自评报告被中断。
- **已落回代码的改进**：
  - 状态徽章颜色按 `docs/design-system.md` 整改：ok=绿 `#5BA86E`、processing=琥珀 `#E8943A`、failed=红 `#C24A3A`；在 `index.css` 新增 `--status-ok`/`--status-amber`/`--status-err` 三对 token，`KnowledgeBaseCard` 全部改引用 CSS 变量。
  - `KbSearchInput` 新增一键清除按钮（`X` 图标，带 `aria-label` 与焦点环）。
  - `KnowledgeBasesPage` 在 `document.title` 之外动态设置 `meta[name="description"]`。
  - 新增 `KbSearchInput.test.tsx`（3 项单测）。
- **验证**：`npm run build` 通过；`npm run test` 31 passed。
- **对比截图**：`.workbuddy/kb_compare_v7.png` / `kb_mobile_compare_v7.png` 已生成，并归档到 `docs/screenshots/`（`kb_real_desktop_v7.png`、`kb_real_mobile_v7.png`、`kb_compare_v7.png`、`kb_mobile_compare_v7.png`）。
- **评估报告**：`docs/knowledge-base-evaluation-report.md` → 12 维体系 **9.21/10（A 级）**；项目既有 6 维体系 **9.46/10**。
- **未达 9.5 的剩余缺口**：D8 安全、D7/D10 单测/可观测、D12 SEO 框架——均属工程基础设施，单页继续投入的边际收益低；建议把同等标准推进到 Task #80–#84 以提升整体项目分。
- **决策**：Task #79 标记完成，进入 Task #80 KBDetail 同步或按用户指定继续。

## 2026-07-13 晚上：KBDetail 详情页同步（Task #80 ✅）
- 依据 `docs/knowledge-base-detail-design-review.md` 的"真实代码层待办"逐项同步：
  - `index.css` `.doc-badge-*` 颜色从旧字面量（ok=棕 `#6e5d4d`、wait=赤陶 `#cb6b3d`、err=深赤陶 `#b85a2e`）改为设计 token：绿/琥珀/红，与 `docs/design-system.md` 硬规则#3 一致。
  - `DocumentTable` 7 个 `<th>` 加 `scope="col"`（a11y）。
  - `.data-table` 由 `border-collapse` 改为 `border-separate border-spacing-0`，使 `border-radius` 真正生效（D5 圆角表格）。
  - `KnowledgeBaseDetailPage` 补动态 `document.title`（默认 + 加载 KB 名后）和 `meta[name="description"]`（SEO）。
  - 新增 `DocumentStatusBadge.test.tsx`（4 项），覆盖 completed/queued/processing/failed → 正确徽章类。
- **验证**：`npm run build` 通过；`npm run test` 35 passed。
- **对比截图**：Playwright 登录 demo_admin 后从 `/knowledge-bases` 点击进入第一个 KB，截取真实详情页与 `docs/knowledge-base-detail-warm-white-preview.html`；左右对比图已归档到 `docs/screenshots/`（`kb_detail_compare_v2.png`、`kb_detail_mobile_compare_v2.png` 等）。
- **评估报告**：`docs/knowledge-base-detail-evaluation-report.md` → 12 维体系 **9.25/10（A 级）**；项目既有 6 维体系 **9.47/10**。
- **未达 9.5 的剩余缺口**：D8 安全审计、D7 单测深度（分页/筛选/权限流程）、D10 可观测性、D12 SEO 框架（canonical/og）、H1 超长文件名 `overflow-wrap` 卫生项——继续单页投入边际收益低；建议推进 Task #81–#84 提升整体项目分。
- **决策**：Task #80 标记完成，进入 Task #81 Members+Departments 同步或按用户指定继续。

## 2026-07-13 夜间：Members + Departments 同步（Task #81 ✅）
- 依据 `docs/members-scorecard.md` / `docs/org-departments-scorecard.md` 的"同步回代码待办"逐项核对真实 React 代码后同步：
  - `DepartmentTree.tsx`：`▸`/`▾` 几何字符 → `lucide-react` SVG Chevron（右→下旋转 90°）；子部门计数徽章冷蓝 `rgba(30,58,95,0.08)` → 暖中性 `#F2EDE7`。
  - `AddUnitMemberDialog.tsx`：「设为主部门」原生 checkbox 加 `accent-[var(--action)]`（选中态染赤陶）。
  - `TransferOwnershipDialog.tsx`：空态提示 / select focus-ring 棕 `#8B5A42` → 品牌 `--action` 赤陶。
  - `MembersTable.tsx` / `UnitMembersTable.tsx`：操作按钮文字色棕 → 赤陶 `#B85A2E`（与删除按钮同源）；所有 `<th>` 补 `scope="col"`。
  - `MembersPage.tsx` / `OrgDepartmentsPage.tsx`：补 `document.title` + 动态 `meta[name="description"]`（SEO 与其他已同步页对齐）。
- **有判断未改动项**：① 角色徽章继续用琥珀色（M-R1-3 / OD-R1-6，产品决策待 `design-system.md` 增角色专用色）；② 原生 `<select>` 保留（a11y 优于非完整 listbox 自定义控件）。
- 新增 `DepartmentTree.test.tsx`（5 项），覆盖渲染、SVG chevron 无几何字符、折叠展开、暖中性徽章、选中回调。
- **验证**：`npm run build` 通过；`npm run test` 40 passed。
- **对比截图**：Playwright 登录 demo_admin 并切换 team workspace（`localStorage.setItem('zhian-workspace', '0f121650-e48f-4954-ab7c-f34f20be4930')`）后截取 `/organization/members` 与 `/organization/departments` 真实页；左右对比图已归档到 `docs/screenshots/`（`members_compare_v3.png`、`members_mobile_compare_v3.png`、`dept_compare_v3.png`、`dept_mobile_compare_v3.png`）。
- **评估报告**：`docs/members-departments-evaluation-report.md` → 12 维体系 **9.01/10（A 级）**；项目既有 6 维体系 **9.16/10**。
- **未达 9.5 的剩余缺口**：角色徽章专用色未决、全站 SEO 框架（og/canonical/JSON-LD）、操作按钮类 inline 重复、埋点 hook、Lighthouse 实测；继续单页投入边际收益低。
- **决策**：Task #81 标记完成，进入 Task #82 Account+OrgSettings 同步。

## 2026-07-13 深夜：Account + OrgSettings 同步（Task #82 ✅）
- 依据 `docs/account-settings-scorecard.md` / `docs/organization-settings-scorecard.md` 的同步回代码待办逐项核对：
  - `components/auth/PasswordStrengthBar.tsx`：修复登录页私有 token 漂移 bug——原用 `--auth-strength-*` / `--auth-line` 仅在 `.auth-page` 作用域定义，在账号设置页（无 `.auth-page`）变量未定义导致颜色失效；改为全局统一状态色 `--status-err` / `--status-amber` / `--status-ok`（文字用对应 ink token），颜色完全不变但作用域正确。
  - `pages/AccountSettingsPage.tsx`：补 `h2 font-serif text-xl` 页标题 + 动态 `document.title` + `meta[name="description"]`。
  - `pages/OrganizationSettingsPage.tsx`：补 `h2 font-serif text-xl` 页标题 + 动态 `document.title` + `meta[name="description"]`。
  - `components/settings/LeaveTeamForm.tsx`：确认离开对话框的"确认离开"按钮由 `#B85A2E` 改为 `var(--status-err)`，符合设计系统 §4.1 销毁型操作走 danger 红。
  - 新增 `PasswordStrengthBar.test.tsx`（5 项），覆盖空密码、meter 段数、弱/强 token 映射、aria-valuenow。
- **有判断未改动项**：OrgSettings 保存成功提示保持暖中性背景（未按预览改为 success 绿），遵循用户"克制、避免过度装饰"偏好；角色/权限徽章保持源码既有方案。
- **验证**：`npm run build` 通过；`npm run test` 45 passed。
- **对比截图**：Playwright 登录 demo_admin，账号设置走 personal workspace，团队设置切 team workspace（`localStorage.setItem('zhian-workspace', '0f121650-e48f-4954-ab7c-f34f20be4930')`），截取真实页与对应 warm-white preview；左右对比图已归档到 `docs/screenshots/`（`account_settings_compare_v1.png`、`account_settings_mobile_compare_v1.png`、`org_settings_compare_v1.png`、`org_settings_mobile_compare_v1.png`）。
- **评估报告**：`docs/account-organization-settings-evaluation-report.md` → 12 维体系：账号 **9.21/10（A 级）**、团队 **9.29/10（A 级）**。
- **未达 9.5 的剩余缺口**：D5 视觉美学、D6 性能未实测、D8 安全审计、D10 埋点/可观测、D12 OpenGraph/canonical 框架——继续单页投入边际收益低；建议推进 Task #83（AdminAudit + DocPreview）或先补全站基础设施后再跑一轮 Lighthouse。
- **决策**：Task #82 标记完成，进入 Task #83 AdminAudit + DocPreview 同步或按用户指定继续。

## 2026-07-13 深夜：全站多维度专业评分（真实代码实测版 ✅）
- 用户「对前端进行多维度的专业评分」。按 `docs/scoring-standard-v3.md`（12 维硬阈值/未测=0/一票否决）对**同步回真实代码 + 性能优化后**的全站重评。关键：D6 性能由「9.0 起评占位」升级为真机 gzip Lighthouse 实测。
- 程序化取证：201 源文件/101 组件/12 页/CSS 1693 行；vitest **55 用例全绿**（新鲜跑）；`console.log`/`eval`/`document.write` 均 0；`!important` 仅 8 处且全在 reduced-motion 守卫+移动抽屉（合理）；`dangerouslySetInnerHTML` 仅 1 处（`SearchSnippet.tsx` 渲染 Postgres `ts_headline`，后端转义仅 `<mark>`，受控）；图标全 lucide SVG；`lang=zh-CN`+viewport+og/twitter 齐；缺 canonical/og:image/robots/sitemap（→SEO 91）。
- 结果：**加权 9.07 / A 档**，无一票否决。逐维：D1 9.3 / D2 9.2 / D3 9.3 / D4 9.1 / D5 8.7 / D6 8.6 / D7 9.3 / D8 8.9 / D9 9.1 / D10 9.1 / D11 9.0 / D12 8.6。短板=D6(LCP 2.2-2.8s 边缘)、D12(SEO 元数据)。比静态均值 9.21 诚实回落 0.14（真机取代占位分）。
- 冲 S 路径：①补 robots/sitemap/canonical/og:image ②登录页补 `<h1>` ③modulepreload+font-display 压 LCP<2.5s。全做约 9.18。
- 产物：`docs/frontend-multidimensional-scorecard.md`（报告）+ 雷达可视化。

