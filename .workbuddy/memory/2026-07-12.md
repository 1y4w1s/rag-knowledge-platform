# 2026-07-12 知岸 RAG 前端打磨（对话页收尾）

## 完成项（"开始打磨"收尾，对应 Task #71–#76）
- **P0 本地化**：新建 `frontend/src/lib/localize.ts`，精确映射后端英文拒答/错误串为中文；接入 `ChatMessageList`(渲染) 与 `api-error.ts`(`readApiErrorDetail`)。仅对已知固定话术做精确/前缀匹配，不误伤用户真实英文答案或合法 detail。
- **P1 键盘可达性**：AskPage / ChatPage 加 `skip-link` 跳到 `#chat-input`；`index.css` 补 `.skip-link`（视觉隐藏 + focus 可见）；`ChatInput` textarea/input 加 `id="chat-input"`。
- **P1 移动端顶栏**：AskPage toolbar 精简（移动端新建按钮 icon-only、`当前空间` 标签 `hidden sm:inline`）；`AppTopbar` breadcrumb 容器加 `truncate` 防折行。
- **P1 清理**：删除 `preview-chat-real.tsx` / `preview-empty-chat.tsx`，移除 `/preview-chat-real`、`/preview-empty-chat` 路由与 import；docs 临时 `.py` 与瞬态截图清理，仅保留 `eval_*` / `final_*`。
- **P2 SEO**：`frontend/index.html` 补 `description` / `theme-color`(#cb6b3d) / `og:*` / `twitter:*` 元数据，title 改为「知岸 · 企业知识库智能问答」。

## 验证
- `npm run build`（tsc -b + vite build）通过，无类型/构建错误；`npm run test`（vitest）28 passed。
- 历史按钮折叠/展开：`threadPanelCollapsed` 状态，桌面默认展开、移动默认折叠，CSS width 过渡 + Escape 关闭。
- 注：英文→中文本地化的实机对话验证需后端 + LLM 返回精确英文串，环境相关；逻辑已静态验证接入点。

## 待办 / 备注
- 实机对话验证本地化触发（需后端可达）。
- Ardot 设计技能（ardot-design-core / ardot-design-router）本回合已加载，但无设计文件 directive，未操作画布；如需同步画廊/画布 mockup 需提供 file URL。
