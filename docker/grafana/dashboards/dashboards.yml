# Grafana 仪表盘 provisioning — 自动导入预置面板
apiVersion: 1

providers:
  - name: "索隐"
    orgId: 1
    folder: "索隐"
    type: file
    disableDeletion: true
    editable: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
