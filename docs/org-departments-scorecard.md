# 组织与部门页 · 评分卡（OrgDepartmentsPage Round 1）

> 标尺：`docs/design-system.md`（统一设计系统）+ `docs/iteration-loop.md` 门禁（六维均 ≥ 8.0）
> 预览：`docs/org-departments-warm-white-preview.html`
> 评审日期：2026-07-12 · 方法：专业评审 → 总结痛点 → 修改预览 → 打分

## 本轮真实痛点与落地（② 总结 → ③ 修改）

| 编号 | 严重度 | 痛点（对照源码） | 修复 | 状态 |
|---|---|---|---|---|
| OD-R1-1 | 🟠 P2 | `AddUnitMemberDialog` 用 2 个**原生 `<select>`**（成员 / 部门角色），破坏设计系统一致性 | 全部改为自定义 `.select`（trigger + listbox + 点击外部关闭 + aria-expanded） | ✅ |
| OD-R1-2 | 🟠 P2 | `DepartmentTree` 折叠箭头用几何字符 `▸`/`▾`，与统一 SVG 图标风格冲突 | 改为线性 SVG chevron（右→下旋转 90°，`currentColor`） | ✅ |
| OD-R1-3 | 🟠 P2 | 子部门计数徽章用蓝色 `rgba(30,58,95,0.08)`，与暖色系统漂移 | 改为中性 `.count-badge`（`--surface-2` 底 + `--line` 边 + `--ink-3` 字），消除冷色 | ✅ |
| OD-R1-4 | 🟠 P2 | 详情面板操作按钮用棕色 `#8B5A42`（非品牌赤陶），跨页动作色不一致 | 统一为 `.btn-ghost`（`--terracotta`），与详情/成员页动作同源 | ✅ |
| OD-R1-5 | 🟡 P3 | 「删除部门」用品牌赤陶（源码销毁型语义决策） | **保全源码**，记台账待产品确认 | ⏸ 已知 |
| OD-R1-6 | 🟡 P3 | 部门角色徽章复用琥珀 `doc-badge-wait`（=处理中状态色），语义碰撞 | **保全源码保真**（同 MembersPage M-R1-3），记台账 | ⏸ 已知 |
| OD-R1-7 | 🟠 P2 | 「设为主部门」原生 checkbox 样式 | 改为自定义 `.check`（勾选态染赤陶 + 键盘可达） | ✅ |

> **无死控件**：树折叠/选中、重命名（实时改树标签与标题）、成员升/降/设主/移出（事件委托，动态重渲染后仍生效）、两对话框（Esc/遮罩关闭、提交关闭）、自定义 select/checkbox 全部可交互。

## ④ 打分（门禁核对）

| 维度 | 权重 | 得分 | ≥ 8.0 | 说明 |
|---|---|---|---|---|
| 一致性 | 20% | 9.3 | ✅ | 外壳/图标/计数徽章/动作色/自定义控件全部对齐系统；与原生 select、冷色徽章漂移已消除 |
| 可用性 | 20% | 9.2 | ✅ | 树折叠+选中、对话框、重命名、成员操作均实时可用 |
| 功能保全 | 20% | 9.6 | ✅ | 五态（管理/未选中/加载/失败/空树）+ 两对话框 + 重命名/删除 + 成员表，覆盖源码全分支 |
| 无障碍 | 15% | 8.8 | ✅ | `th scope=col`、dialog/select/checkbox aria、chevron aria-label、Esc 关闭；自定义 select 非完整键盘 listbox（预览层已知限制，真实 Radix 已处理） |
| 视觉 | 15% | 9.1 | ✅ | 2fr/3fr 分栏清晰，暖白 token 统一，空态留白舒适 |
| 性能 | 10% | 9.0 | ✅ | 纯 CSS 动效 + `prefers-reduced-motion` 守卫，无重渲染风险 |

**加权总分 = 0.20×9.3 + 0.20×9.2 + 0.20×9.6 + 0.15×8.8 + 0.15×9.1 + 0.10×9.0 = 9.21**

## 门禁结论

**六维全部 ≥ 8.0 → 门禁 ✅，加权 9.21。** 组织与部门页达标。

## 跨页待办（非本页缺陷）

- 侧栏工作区切换 `ws-caret` 的 `▾` 为 4 个已达标页共享外壳既有元素，本次为保持跨页一致予以保留；建议后续统一外壳时一并替换为 SVG（影响 KBs/KBDetail/Account/Members/OrgDepartments 五页）。
- 部门角色徽章（OD-R1-6）与成员角色徽章（M-R1-3）复用琥珀，建议在 `design-system.md` §4 增加「角色徽章专用色」以避免与「处理中」状态色碰撞。
